import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.desihire.aiml04',
  appName: 'DesiHire',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
